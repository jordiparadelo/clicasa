import React from 'react';
// styles
import styles from 'styles/Hero.module.scss'

const Hero = () => {
    return (
        <header id="Hero">
            Hero
        </header>
    );
};

export default Hero;